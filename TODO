 + url get title
// Only calling the head
curl_setopt($ch, CURLOPT_HEADER, true); // header will be at output
curl_setopt($ch, CURLOPT_CUSTOMREQUEST, 'HEAD'); // HTTP request is 'HEAD'
http://icfun.blogspot.com/2008/07/php-get-server-response-header-by.html

ALTER TABLE `casimir` ADD `title_url` VARCHAR( 128 ) NOT NULL DEFAULT 'No title defined for this url',
ADD FULLTEXT (
`title_url`
)

ajout parametre pour CDN 	screen.css et casimir.png

